---
import { Picture } from "astro:assets";
import heroImage from "@/assets/hero-source.svg";
import Link from "@/components/ui/link.astro";
import { Icon } from "astro-icon/components";
---

<main
  class="grid lg:grid-cols-2 place-items-center pt-16 pb-8 md:pt-12 md:pb-24">
  <div class="py-6 md:order-1 hidden md:block">
    <Picture
      src={heroImage}
      alt="Helping hand illustration"
      widths={[200, 400, 600]}
      sizes="(max-width: 800px) 100vw, 620px"
      loading="eager"
      format="avif"
    />
  </div>
  <div>
    <h1
      class="text-5xl lg:text-6xl xl:text-7xl font-bold lg:tracking-tight xl:tracking-tighter">
      OpenHand – Giving Clothes a Second Life
    </h1>
    <p class="text-lg mt-4 text-slate-600 max-w-xl">
      OpenHand is a non-profit association created in 2025 in Blonay – Saint-Légier. 
      Our mission is simple: <strong>collect and distribute second-hand or new clothing 
      to people in need</strong>.
    </p>
    <div class="mt-6 flex flex-col sm:flex-row gap-3">
      <Link
        href="#events"
        class="flex gap-1 items-center justify-center"
        rel="noopener">
        <Icon class="text-white w-5 h-5" name="bx:calendar" />
        View Upcoming Events
      </Link>
      <Link
        size="lg"
        style="outline"
        rel="noopener"
        href="#"
        class="flex gap-1 items-center justify-center"
        target="_blank">
        <Icon class="text-black w-4 h-4" name="bx:heart" />
        Join the Movement
      </Link>
    </div>
  </div>
</main>
